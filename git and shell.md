## 项目开发流程
1. 需求方案 
2. 产品方案 
3. 产品评审
4. 确定方案 
5. 技术调研 
6. 确定排期 
7. 进行开发 
8. 各开发组分工 
9. 对接口 
10. 部署测试服务 
11. 线上部署 
## shell与vim 流行框架的知识点 
+ git与工具 
+ angular 
### 主要的知识点 
1. 基本的Linux命令 
2. 基本的vim操作 
3. git的使用 
4. 构建工具gulp的使用 
## 操作系统发展简介 
1. 巨型机 
2. 批处理机 
3. MULTICS 多用户信息通信系统 
4. Unix 
5. Dos 
6. Windows 与 Mac OS 
==========================================================================================================================
## shell命名
   + 增 > >> touch  cp vim
   + 删 rm rmdir
   + 改 mv
   + 查 list cat head tail more less

## 版本控制工具
  + 字面意思：就是管理和维护（每次更新，修改，增加，删除，多人开发，合并冲突）源代码的工具
  + 版本控制工具的分类：
    - 本地管理：版本过多，不利于维护查找，不利于团队协作，不利于分发代码
    - 集中管理：svn(subversion),服务器压力大
    - 分布式管理 服务器与本地的库进行同步，凡是设置到分布的概念，就是将复杂的集中式计算分发给每个个体计算最后合并
## shell 命令基础详细使用
 1. 入门标准 
    - 所有的关键字语法 
    - 字符串操作 
    - 集合操作(数组，键值对) 
    - 文件操作 
 2. 命令的一般格式 
    - $ 命令名[-选项][参数] 
    - cd 目录路径 
    - ls 列出当前目录下的所有的 
    - ./ 当前目录 
    - ../上级目录 
    - \n 换行回车 
    - # 命令行界面介绍 
    - ~ 家目录 
    - $ 引导
 > 在命令行中键入一个命令，回车执行，实际上是通知shell去执行硬盘上的一个程序，这个命名名就是程序的名字 当在命令行键入一个命令的时候，会在当前文件夹下找是否有这个程序，如果没有，就会在一个叫系统变量的变量中找存储的路径，看这些路径是否有这个命令的程序

 > 系统变量：
用户环境变量 C:\Users\李爱珍\AppData\Roaming\npm;%USERPROFILE%\AppData\Local\Microsoft\WindowsApps; 命令行的搜索路径存储在系统的Path环境变量中，系统--高级设置，如果需改环境变量，需要重启命令行 git-downloads 下载git
 > 在Linux中所有的操作都要求可以简化，所以一般选项都带有短名字，一个减号链接的是短名字，两个减号链接的是长名字。 
 3. 常用命令 
 + pwd print working directory 打印当前文件夹路径 
 + mkdir make directory 文件夹名  创建文件夹 
 + rm 文件夹名 移除空文件夹 
 + rm -rf 文件夹名 强制删除非空文件夹 
 + touch 文件名 创建文件
 + 复制与移动的操作
    - cp 文件1 文件夹 文件夹必须存在 
    - mv 文件1 文件夹 移动文件到文件夹中 
    - mv 文件夹1 文件夹2/ 将文件夹1移动到文件夹2下 
    - mv 文件夹1 文件夹2 将文件夹1的名称改成文件夹2
 + wc word count ascii码 双字节字符 
 + 字 unicode 编码 
 > 凡是字符都用两个字节表示 
 + utf格式：utf-8编码 汉字占3个字节 
 + 字符：一个字节表示 字：多个字节表示
 + cat 文件名 功能：将文件与终端相连，意味着将文件的内容显示在终端中 
 > $ cat 1.txt 2.txt 3.txt>index.txt 将三个文件的内容拼在一起后保存在index.txt文件中
 + less 分屏显示 ,more 分屏显示，有一个百分比，Windows不支持more 
 + head 默认显示前十行 -n（显示到第几行） 
 + tail 倒数十行 -n -f(同步文件的内容) 
 + history:打印出到现在键入的命令数 
 + echo 输出 重定向
 + >将左边命令成成的结果写入到指定的文件中, 如果文件存在则覆盖文件 
 + >> 将左边的命令生成的文本写入文件, 如果文件存在则追加
 ## vim编辑器的使用
  + 打开  vim filename
  + 关闭  :q
  + 保存  :w
  + 另存  :w filename
  + 强制执行 :w!
### vim编辑器是有多种状态 
1. 命令模式，输入模式（i）vim，底行模式(:) 
2. 命名模式，输入模式 
================================================================================================================================
## git 的使用
### 工作原理：
   + 在git项目中，含有一个本地数据库，会记录每一次对代码的修改。
   + git的操作：
    - 加入修改（将程序员对代码做的告诉git，让git去记录用户的修改（git add）,这里的修改只是加入到git过提供的缓存中，并没有真正的记录下来）
    - 提交修改（提交的是真正的修改）就是将当前程序员的修改保存到git中本地数据库中。
    - 将一个项目分成三个部分
        1. 工作区：写代码的地方
        2. 缓存区
        3. git仓库
### 完整的git管理来学习基本的git命令
#### git的基本命令
+ 初始化一个仓库
+ 在工作区编写代码
+ 告诉git来管理所有的代码
    - git add .  .代表当前文件
    - git add *
    - git add -A
+ 告诉git在数据中存储所有的修改（一定要设置用户的信息）
    - git commit -m "消息"
+ 回退到某个版本
    - git reset -hard SHA1值
+ 其他的命令
    - git status
    - git log
    - git reflog
#### git的核心原理（1）
    - 配置信息
        1. git config user.name ''
        2. git config user.email ''
        3. --global 表示全局，也就是说只要是使用该计算机来操作git，那么就默认使用该用户名与邮箱。
        4. 不带golbal表示局部，意味着使用文件夹作为单位，用户名与邮箱只在文件夹使用，如果一台计算机中同时都有，那么局部优先。
        5. 如果使用的是全局设置用户信息，那么就会在家目录下生产一个.gitconfig文件，里面存储着制定的信息
 #### 分支（branch）
   > 什么时候需要使用分支
    凡是涉及到对代码不安全的任何操作，都应该对代码进行分支处理，创建新的分支，在保证新的分支完成没有问题的时候，在与主分支合并，可以将新分支删除。
   > 不安全操作
    所谓的不安全操作，不是指不好的操作，而是说会将代码进行修改，而对当前状态发生变化的操作。
   > 一般情况在企业中
    一个项目经理会新建一个项目仓库，然后所有的程序员被分配代码开发。
    项目经理设计仓库，创建master分支，然后创建developer分支（debug分支等）。
    每一个程序员在developer分支上开发，每一个程序员自己还有维护分支。程序员将代码处理完毕后，提交合并请求。然后项目经理会安排测试，如果没有问题就与master合并。
   > 实际操作
#### 分支的操作
  + 查看分支：git branch ,当前分支会标有一个星号
  + 创建分支：git branch 分支名
  + 切换分支：git checkout 分支名
  + 创建+切换：git checkout -b 分支名 创建并且切换到新创建的分支
  + 合并分支：git merge 分支名。将分支与当前分支合并
  + 删除分支：git branch -d 分支名

#### git的核心原理（2）
  + 远程仓库
  + 在自己的计算机中利用文件夹模一下远程结构
  + 用git.oschina.net 来实现一下
  + git init --bare 裸露的仓库，就是没有工作区
  + git clone 克隆远程仓库的内容
  + cd 进入或者回到回退
  + 推送  git push ../../services master
## git使用总结
 + 基本使用
    - git init /git init --bare
    - git add 文件名/*
    - git commit -m'说明文字'
    - 创建远程仓库，最好必要勾选生产README文件，不然很难推送成功
    - git config - - global user . email '123456@qq.com' 
    - git config - - global user . name 'saly'
    - git remote add origin git@github.com:liaizhen/git-use.git 建立与远程仓库的联系
    - git push -u origin master 推送  origin master是你仓库的小名，-u是第一次推送才需要
    =======================
  + 二次推送或者多次推送，只需要执行以下命令
    - git add 文件名/*
    - git commit -m'说明文字'
    - git pull git@github.com:liaizhen/git-use.git
    - git push origin master
  > 远程推送的时候可能会被拒绝，有可能是网络原因，如果被无原因被rejected,可以 git pull --rebase后再push
